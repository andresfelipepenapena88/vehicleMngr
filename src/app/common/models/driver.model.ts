export interface DriverBasicInfo {
    id: string;
    name: string;
};

export interface DriverInfo {
    docRef?: string;
    id: string;
    name: string;
    phone?: string;
    address: string;
    email?: string;
}