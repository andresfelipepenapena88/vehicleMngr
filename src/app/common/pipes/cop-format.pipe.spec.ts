import { CopFormatPipe } from './cop-format.pipe';

describe('CopFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CopFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
